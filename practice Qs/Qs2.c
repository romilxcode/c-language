// write a program to calculate area of circle. (radius is given)

#include<stdio.h>
//area of square
int main() {
      float radius;
      printf("enter side");
      scanf("%f", &radius);
      printf("area is : %f", 3.14 * radius * radius);
      return 0;
}