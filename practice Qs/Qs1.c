//Q.1 Write a program to calculate area of square (side is given)

#include<stdio.h>
//area of square
int main() {
      // int side;
      float side;
      printf("enter side");
      // scanf("%d", &side);
      scanf("%f", &side);
      // printf("area is : %d", side * side);
      printf("area is : %f", side * side);
      return 0;
}