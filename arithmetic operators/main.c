#include<stdio.h>

int main() {

      // arithmetic operators = + - * / % ++ --

      int x = 10;
      int y = 3;
      // float z = 2;
      int z = 0;

      // z = x + y;
      // z = x - y;
      // z = x * y;
      // z = x / y;
      // z = x % 2;

      // x++;
      // x--;
      // x+=3;
      x-=3;

      printf("%d", x);

      return 0;
}