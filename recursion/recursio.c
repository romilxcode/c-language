// Recursion:
// When a function calls itself, it's called recursion.

// Base Case – Condition under which the function stops calling itself (to prevent infinite loop).
// Recursive Case – The part where the function calls itself with a smaller/simpler input.